import React from 'react';

import { ToastContainer, toast } from 'react-toastify';

const Toast = (notify) => {
    return (
        <div>
        <ToastContainer />
      </div>
    );
};

export default Toast;